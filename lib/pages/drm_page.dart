import 'package:flutter/material.dart';

class DRMPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("DRM Protection")),
      body: Center(child: Text("DRM and Watermarking in progress")),
    );
  }
}
